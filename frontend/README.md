# Frontend

React + Tailwind chat client for the AI assistant.

## Commands

```bash
npm install
npm start
npm run build
```

## Environment

Create `.env` from `.env.example`:

```bash
cp .env.example .env
```

- `REACT_APP_API_URL` defaults to `/api/v1` on current host if empty.
